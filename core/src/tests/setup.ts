/**
 * Vitest Setup File
 *
 * The cloudflare:workers module is mocked via an alias in vitest.config.ts
 * This allows Durable Object classes to be instantiated in Node.js tests.
 */

// No additional setup required - cloudflare:workers is aliased in vitest.config.ts
